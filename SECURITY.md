# 安全政策（Security Policy）

## 支持范围

| 版本 | 是否支持 |
| --- | --- |
| 0.1.x | 支持（当前开源版本） |

本项目当前开源 **Windows（x64 为主，arm64 为实验性）** 与 **Android** 两个版本。其他平台不在本次开源范围内，也不在安全响应范围内。

## 报告漏洞

请通过 GitHub 的 **Security → Report a vulnerability** 私下提交，或直接在仓库开 Private Security Advisory。**请勿在公开 Issue 中贴出可利用细节。**

报告时请包含：

- 影响版本 / 平台（Windows x64 / Windows arm64 / Android）
- 复现步骤与最小复现用例
- 影响面（本地数据泄露 / 任意代码执行 / 拒绝服务等）
- 你建议的修复方向（可选）

我们会在确认后给出修复与发布计划。在你公开披露前，请给我们合理的修复窗口。

## 安全模型（重要前提）

Zynthel 是 **local-first 单机应用**，不是云服务：

- 应用**不运营任何后端**，维护者不会收到你的工作区数据；
- 所有数据默认保存在**你自己的设备本地**（Web 视图 localStorage / IndexedDB）；
- 项目**不内置任何 AI 服务端点或密钥**，AI 为纯配置入口，你填什么就直连什么；
- 因此“服务端漏洞”“数据库泄露”这类问题在本项目中**不存在**。

## 已知限制（不属于漏洞，请勿作为漏洞上报）

1. **Windows 安装包未签名。** 项目没有商业代码签名证书，安装时 Windows 会提示“未知发布者 / SmartScreen 筛选器”。这是已知的分发限制，**不会**通过降低系统安全设置来规避。请自行校验发布页提供的校验值后再安装。
2. **Android APK 未签名发布版。** CI 产出的是 debug 变体 APK（带 `.debug` 包名后缀），仅用于自编译安装 / sideload，不适用于应用商店分发。
3. **AI API Key 存放在应用本地存储，不是操作系统安全密钥库级别的保护。** 请勿在不受信任的设备上配置高价值密钥。详见 README 的「数据与隐私」。
4. **`QUERY_ALL_PACKAGES` 权限**是 App Picker 枚举本机已安装应用所必需的，属于有意设计；本项目定位为 GitHub 开源 + sideload，不做应用商店上架。
5. **密码本主密码遗忘不可恢复。** 加密密钥只存在于内存，项目不保存、也无法帮你找回主密码。

## 不在响应范围内

- 未签名安装包导致的 SmartScreen / “未知发布者”提示；
- 你自己配置的第三方 AI 服务的行为、计费、隐私与合规问题（请联系该服务商）；
- 缺少 Google Play 上架资质（`QUERY_ALL_PACKAGES` 受限权限）；
- 通过修改源码、注入脚本或第三方插件引入的问题。

## 依赖安全

前端依赖由 `pnpm-lock.yaml` 锁定，Rust 依赖由 `src-tauri/Cargo.lock` 锁定。如发现依赖存在已知 CVE，欢迎直接提 Issue 或 PR，并附上 CVE 编号与影响分析。
